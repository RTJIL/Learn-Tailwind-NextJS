// --- IGNORE --- This file is used to show a loading state while the dashboard data is being fetched.
import DashboardSkeleton from "@/app/ui/skeletons";

export default function Loading() {
  return <DashboardSkeleton />;
}
